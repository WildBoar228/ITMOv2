# Integration-проверки

| Связь компонентов | Что может сломаться | Как воспроизводим | Ожидаемый результат | Evidence |
|---|---|---|---|---|
| API ↔ ReviewService | Неверная схема ответа | Вызываем POST /api/reviews с корректным diff | 200 и JSON соответствует OUT-1 | Контрактная проверка |
| ReviewService ↔ LLM | Утечка секретов | В diff включаем token=TEST; проверяем prompt в заглушке | В prompt секрет заменён на [REDACTED] | Лог заглушки LLM (без diff целиком) |
| API ↔ валидация | Пропуск лимита размера | Отправляем diff длиной 20001 | HTTP 413 | Ответ и код |
| API ↔ валидация | Отсутствует diff | Отправляем тело без поля diff | HTTP 422 | Ответ и код |
| ReviewService ↔ LLM | Таймаут/ошибка внешнего вызова | Заглушка возвращает ошибку/зависает | Контролируемый ответ в ≤10с | Засечённое время |

## Как использовали AI

- Строка в [`prompts.md`](prompts.md): P1-04
- Что проверили и исправили сами: учтены все 3 основные проблемы из ревью
