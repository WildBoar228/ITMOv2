# Chain of Verification

- Проверяемый черновик или утверждение:
Файл `practices/practice_01/tests_load.md` в текущем виде содержит только 2 сценария с произвольными порогами задержек и не покрывает ключевые требования: REL-1 под нагрузкой (таймаут/контролируемый ответ в <=10с и соответствие OUT-1), воспроизводимое окружение и метрики, а также отказные сценарии под нагрузкой для API-1 (413) и валидации (422).

## Запрос на проверку
Проверь текущий `practices/practice_01/tests_load.md` по трём пунктам и предложи минимальные правки:
1) Есть ли сценарии, моделирующие ошибки/таймауты внешнего LLM под нагрузкой, с проверкой, что контролируемые ответы приходят в <=10 сек (REL-1) и соответствуют OUT-1? Если нет — добавь отдельный сценарий и критерии pass/fail, привязанные к правилам CASE.
2) Описано ли воспроизводимое окружение (аппаратные ресурсы, конкуренция/число воркеров, версия раннера, профиль заглушки LLM), список метрик (p50/p95/p99 latency, error rate с разбивкой 4xx/5xx, CPU/RAM, очередь/ретраи) и явные артефакты Evidence (имена отчётов/логов)? Если нет — добавь разделы Environment и Metrics & Evidence.
3) Есть ли отказные сценарии под нагрузкой для API-1 (413 при diff > 20000) и 422 (нет поля diff), с требованием 100% корректных кодов при нагрузке и без произвольных latency-порогов? Если нет — добавь.

После проверки внеси минимальные правки в `tests_load.md`, сохрани исходный тон и структуру, убери произвольные пороги p95 и свяжи pass/fail с правилами CASE (REL-1, API-1, OUT-1). Таблицу сценариев расширь до 3–4 пунктов.

## Вопросы проверки и evidence

| Вопрос | Источник или проверка | Результат |
|---|---|---|
| Соответствуют ли критерии REL-1 (<=10с и контролируемый ответ) под нагрузкой? | CASE.md: REL-1 (стр. 66–71); поиск сценария error storm | Изначально отсутствовало; добавлен сценарий «Error storm (REL-1)» с pass/fail ≤10с и OUT-1 |
| Проверяется ли соответствие OUT-1 под деградацией? | CASE.md: OUT-1 (стр. 67–71); проверка Evidence на валидацию OUT-1 | Изначально отсутствовало; добавлен out1_check.log и явные критерии соответствия OUT-1 |
| Есть ли чётко описанное Environment? | Проверка наличия раздела Environment в tests_load.md | Изначально отсутствовало; добавлен раздел Environment |
| Зафиксированы ли p50/p95/p99, error rate c разбивкой 4xx/5xx, CPU/RAM? | Проверка раздела Metrics & Evidence | Изначально частично (неявно); добавлен раздел Metrics & Evidence и артефакты |
| Есть ли сценарии под нагрузкой для 413 (API-1) и 422 (валидация)? | CASE.md: API-1 (стр. 65); артефакты Практики 1 | Изначально отсутствовало; добавлен сценарий «Validation under load (API-1/422)» |

## Исправленный результат

См. изменённый файл: [tests_load.md](tests_load.md)

## Что изменили в исходном артефакте

- Файл и раздел:
`practices/practice_01/tests_load.md`, добавлены разделы "Environment" и "Metrics & Evidence"; обновлена таблица сценариев.
- Изменение:
Удалены произвольные пороги p95 (≤ 500 мс/1 с). Добавлены сценарии: "Error storm (REL-1)" с контрольной деградацией ≤10с и соответствием OUT-1; "Validation under load" для API-1 (413) и 422 с 100% корректных кодов под нагрузкой. Связали pass/fail с правилами CASE (REL-1, OUT-1, API-1). Уточнили Evidence-артефакты.
- Что отклонили:
Отклонены добавление новых бизнес-SLO сверх CASE, произвольные целевые задержки, полноценная нагрузка на реальный LLM из-за стоимости/лимитов.

## Как проверили

Сопоставили критерии с CASE.md (строки 64–71); убедились, что Evidence-артефакты не включают содержимое diff (соблюдение SEC-1/OBS-1); проверили наличие метрик p50/p95/p99 и разбивки ошибок 4xx/5xx.
