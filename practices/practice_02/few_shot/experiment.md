# Few-shot

- Артефакт Практики 1:
`practices/practice_01/tests_load.md`
- Что хотим улучшить:
Добавить сценарии под REL-1 (таймауты/ошибки внешнего LLM под нагрузкой с контролируемыми ответами и соответствием OUT-1), описать воспроизводимое окружение и метрики/артефакты Evidence, добавить отказные сценарии под нагрузкой для API-1 (413) и 422.

## Примеры

### Хороший результат
Таблица сценариев включает:
- Error storm: заглушка LLM выдаёт таймауты/ошибки при RPS X, проверяется ответ <=10с и соответствие OUT-1; Evidence: файл отчёта с p50/p95/p99, error rate (4xx/5xx), CPU/RAM.
- Validation under load: смесь корректных и ошибочных запросов; проверяется 100% 413 для diff>20000 и 100% 422 при отсутствии diff; без произвольных latency-порогов.
- Environment: описаны аппаратные ресурсы, воркеры, раннер, профиль заглушки; метрики и артефакты названы явно.

### Плохой результат
Два сценария с произвольными порогами p95 ≤ 500 мс / 1 с, без описания окружения и без проверки отказных кодов 413/422 под нагрузкой; Evidence: «Отчёт теста» без конкретики.

## Запрос
Сгенерируй правки для `practices/practice_01/tests_load.md`, опираясь на хорошие и плохие примеры выше. Минимальная правка, без переписывания всего файла:
1) Убери произвольные пороги задержки и привяжи pass/fail к CASE-правилам: REL-1 (<=10с и контролируемый ответ, соответствующий OUT-1) и OUT-1.
2) Добавь разделы Environment и Metrics & Evidence: ресурсы/воркеры/раннер/профиль заглушки; метрики p50/p95/p99, error rate 4xx/5xx, CPU/RAM; имена артефактов.
3) Расширь таблицу сценариев: Error storm (REL-1+OUT-1), Validation under load (API-1/422), а также базовые стабильные запросы без произвольных KPI.

## Что получили


## Что изменили в исходном артефакте

- Файл и раздел:
- Изменение:
- Как проверили:
- Что отклонили:
